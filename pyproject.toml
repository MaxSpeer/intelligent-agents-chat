[project]
name = "intelligent-agents-chat"
version = "0.1.0"
description = "A minimal NiceGUI starter for the Intelligent Agents project."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "nicegui>=3.0,<4",
]

[dependency-groups]
dev = [
    "ruff>=0.12",
]

[tool.ruff]
line-length = 100
